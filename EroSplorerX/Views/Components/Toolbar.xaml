<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="EroSplorerX.Views.Components.Toolbar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EroSplorerX.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:materialIcons="using:Material.Icons.WinUI3" 
    xmlns:data="using:EroSplorerX.Data" 
    d:DataContext="{d:DesignInstance Type=data:MainPageViewModel}"
    mc:Ignorable="d" Height="36">

    <Grid>
        <Border CornerRadius="10"  Background="#171717">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <ToggleButton Margin="2" IsChecked="{Binding ShowPlayed}" ToolTipService.ToolTip="Show Played Funscripts">
                        <FontIcon Glyph="&#xE930;" />
                    </ToggleButton>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
